# 🔑 Проверка API ключа Perplexity

## Ошибка 401 Authorization Required

Эта ошибка означает, что Perplexity API не принимает ваш API ключ.

## Шаги диагностики:

### 1. Проверьте формат файла `private.txt` на сервере

```bash
cd ~/chat-filter
cat private.txt
```

Убедитесь, что строка с `PERPLEXITY_API_KEY` выглядит так:

```
PERPLEXITY_API_KEY=pplx-ваш-ключ-здесь
```

**Важно:**
- ❌ Нет пробелов вокруг `=`
- ❌ Нет кавычек вокруг ключа
- ❌ Нет лишних пробелов в начале или конце строки
- ❌ Нет лишних переносов строк

### 2. Проверьте сам API ключ

Войдите в ваш аккаунт Perplexity:
- https://www.perplexity.ai/settings/api

Убедитесь что:
- ✅ Ключ активен
- ✅ У вас есть кредиты/подписка
- ✅ Ключ не истек

### 3. Проверьте, что ключ скопирован правильно

```bash
# Покажет длину ключа
cd ~/chat-filter
cat private.txt | grep PERPLEXITY_API_KEY | wc -c
```

Правильный ключ Perplexity обычно имеет формат `pplx-XXXXX...` и длину около 60-80 символов.

### 4. Пересоздайте API ключ

Если ничего не помогает:
1. Зайдите на https://www.perplexity.ai/settings/api
2. Удалите старый ключ
3. Создайте новый ключ
4. Скопируйте его полностью (без пробелов)
5. Обновите `private.txt` на сервере:

```bash
nano ~/chat-filter/private.txt
# Замените строку PERPLEXITY_API_KEY=...
# Ctrl+X, Y, Enter для сохранения
```

### 5. Перезапустите бота

```bash
cd ~/chat-filter
python3 main.py
```

При запуске вы увидите диагностическую информацию:

```
🔑 Проверка Perplexity API ключа:
   Длина: 65 символов
   Первые 10 символов: pplx-12345...
   Последние 10 символов: ...abcdef123
   ✅ Ключ содержит только ASCII символы
```

Если длина ключа сильно отличается (например, < 50 или > 100 символов), это может указывать на проблему.

## Тестирование API ключа вручную

Вы можете протестировать ключ напрямую:

```bash
curl -X POST https://api.perplexity.ai/chat/completions \
  -H "Authorization: Bearer ВАШ_КЛЮЧ" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sonar-pro",
    "messages": [
      {"role": "user", "content": "test"}
    ]
  }'
```

Если получите `401` - проблема в ключе.
Если получите ответ - ключ работает, проблема в боте.

## Поддержка

Если проблема сохраняется, сообщите:
- Вывод при запуске бота (диагностика ключа)
- Длину ключа
- Дату создания ключа

