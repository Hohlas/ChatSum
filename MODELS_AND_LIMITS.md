# 🤖 Модели AI и ограничения

## ⚠️ ВАЖНОЕ ОБНОВЛЕНИЕ

**Через Perplexity API доступны ТОЛЬКО модели Sonar!**

- ✅ Sonar (базовая)
- ✅ Sonar Pro (улучшенная)
- ❌ Claude - только в веб-интерфейсе Perplexity Pro
- ❌ GPT - только в веб-интерфейсе Perplexity Pro
- ❌ Kimi K2 - только в веб-интерфейсе Perplexity Pro

Если вам нужны Claude или GPT, используйте их напрямую через:
- Anthropic API (для Claude)
- OpenAI API (для GPT)

---

## 📊 Проблема обрезки сообщений - РЕШЕНО!

### Что было раньше (v2.0.0):
```python
max_chars = 20000  # Жесткий лимит
if len(messages_json) > max_chars:
    # Берем только первые N сообщений
    messages_data_limited = messages_data[:limit]  # ❌ Потеря новых данных!
```

**Проблемы:**
- ❌ Терялись последние (самые свежие!) сообщения
- ❌ Пропускались важные обсуждения
- ❌ Нарушался контекст

### Что стало (v2.1.0):

```python
# 1. Динамические лимиты в зависимости от модели
if 'claude' in model.lower():
    max_chars = 400000  # Claude может обработать больше
elif 'sonar' in model.lower():
    max_chars = 250000  # Sonar средний контекст
    
# 2. Берем ПОСЛЕДНИЕ сообщения (самые актуальные)
messages_data_limited = messages_data[-limit:]  # ✅ Сохраняем новые!

# 3. Подробное логирование
print(f"⚠️  ПОТЕРЯ ДАННЫХ: {len(messages_data) - limit} старых сообщений")
print(f"💡 Рекомендация: уменьшите период (/analyze 12h вместо 24h)")
```

**Улучшения:**
- ✅ Берутся самые актуальные сообщения
- ✅ Подробная статистика потерь
- ✅ Рекомендации пользователю
- ✅ Разные лимиты для разных моделей

## 🎯 Что будет БЕЗ ограничений?

### Технические лимиты API:

| Модель | Контекст | Макс. символов | Что случится при превышении |
|--------|----------|----------------|----------------------------|
| **sonar** | 127K токенов | ~500K символов | ❌ API вернет ошибку |
| **claude-3.5-sonnet** | 200K токенов | ~800K символов | ❌ API вернет ошибку |
| **claude-3-opus** | 200K токенов | ~800K символов | ❌ API вернет ошибку |
| **gpt-4o** | 128K токенов | ~500K символов | ❌ API вернет ошибку |

### Пример ошибки:
```
❌ Error: Request too large (exceeds token limit)
💰 Деньги списываются за неудачный запрос
⏱️ Время потрачено впустую
```

## 🚀 Доступные модели (через Perplexity API)

### 1. Sonar (базовая)

**Основа:** Llama 3.3 70B от Meta, оптимизированная Perplexity

**Когда использовать:**
- Большие объемы данных
- Быстрые проверки
- Ограниченный бюджет
- Тестирование

**Характеристики:**
- 💰 Цена: ~$0.20 / 1M токенов (вход и выход)
- ⚡ Скорость: Очень быстро
- 🎯 Качество: Хорошее
- 📊 Контекст: 127K токенов (~500K символов)
- 🔍 Интеграция с поиском Perplexity в реальном времени

**Команда:**
```
/set_model sonar
```

### 2. Sonar Pro ⭐ (РЕКОМЕНДУЕТСЯ)

**Основа:** Llama 3.3 70B (оптимизированная версия)

**Когда использовать:**
- **Криптовалютные чаты** (ВАШ СЛУЧАЙ!)
- Сложная терминология
- Нужны детальные анализы
- Важна точность
- Производственное использование

**Характеристики:**
- 💰 Цена: ~$1.00 / 1M токенов (вход и выход)
- ⚡ Скорость: Очень быстро
- 🎯 Качество: Отличное ⭐⭐⭐
- 📊 Контекст: 127K токенов (~500K символов)
- 🔍 Улучшенная интеграция с поиском
- 🧠 Лучше понимает контекст и нюансы
- ⚡ В 5 раз дешевле чем Claude через Anthropic API

**Команда:**
```
/set_model sonar-pro
```

**Рекомендуется по умолчанию!** ✅

---

## 🚫 Модели НЕ доступные через API

Эти модели доступны ТОЛЬКО в веб-интерфейсе Perplexity Pro:

### Claude Sonnet 4.5 / Claude Opus
❌ Не доступны через Perplexity API
💡 Используйте Anthropic API напрямую: https://www.anthropic.com/api

### GPT-5.1 / GPT-4o  
❌ Не доступны через Perplexity API
💡 Используйте OpenAI API напрямую: https://platform.openai.com/

### Kimi K2
❌ Не доступны через Perplexity API

---

## 💡 Альтернатива для Claude

Если вам действительно нужен Claude для криптовалютных чатов:

**Вариант 1: Anthropic API (рекомендуется)**
```python
# Вместо Perplexity
from anthropic import Anthropic
client = Anthropic(api_key="your-key")
```

**Преимущества:**
- ✅ Прямой доступ к Claude 3.5 Sonnet, Claude 3 Opus
- ✅ Больше контроля
- ⚠️ Дороже (~$3/$15 за 1M токенов)

**Вариант 2: Остаться с Sonar Pro**
```
/set_model sonar-pro
```

**Преимущества:**
- ✅ В 15 раз дешевле чем Claude
- ✅ Интеграция с поиском в реальном времени
- ✅ Хорошее качество для большинства задач
- ✅ Основан на Llama 3.3 70B (сильная модель)

## 💡 Рекомендации по использованию

### Для криптовалютных чатов (ВАШ СЛУЧАЙ):

**Оптимальная конфигурация (через Perplexity API):**
```
/set_model sonar-pro
```

**Почему Sonar Pro:**
1. ✅ Основан на Llama 3.3 70B (мощная модель)
2. ✅ Интеграция с поиском в реальном времени
3. ✅ В 15-75 раз дешевле чем Claude через Anthropic API
4. ✅ Оптимизирован Perplexity для анализа текста
5. ✅ Хорошо работает с технической терминологией
6. ✅ Быстрые ответы

**Если нужно ещё лучше качество:**
- Используйте Claude Sonnet 4.5 **напрямую через Anthropic API**
- Но это будет дороже (~$3-15 за 1M токенов против ~$1 у Sonar Pro)

### Стратегия использования:

#### Рекомендуемая конфигурация:
```bash
# Используйте Sonar Pro для всех анализов
/set_model sonar-pro
```

#### Для ежедневного анализа:
```bash
# Утром - анализ за ночь
/set_model sonar
/analyze 8h

# Вечером - детальный анализ
/set_model sonar-pro
/analyze 12h
```

#### Для важных периодов:
```bash
# Sonar Pro для важных событий
/set_model sonar-pro
/analyze 24h
```

#### При очень больших чатах:
```bash
# Разбиваем на части
/analyze 6h   # Часть 1
/analyze 6h   # Через 6 часов - часть 2
```

## 📈 Примеры расчета стоимости

### Типичный криптовалютный чат за 12 часов:

**Исходные данные:**
- 500 сообщений
- После фильтрации: 300 сообщений (~150K символов)
- Примерно 40K токенов на входе
- Примерно 3K токенов на выходе (детальный анализ)

**Стоимость по моделям (через Perplexity API):**

**Sonar (базовая):**
- Вход: 40K × $0.20 / 1M = $0.008
- Выход: 3K × $0.20 / 1M = $0.0006
- **Итого: ~$0.009 (~0.9 цента)** 💰

**Sonar Pro (улучшенная):**
- Вход: 40K × $1.00 / 1M = $0.04
- Выход: 3K × $1.00 / 1M = $0.003
- **Итого: ~$0.043 (~4 цента)** 💰💰

### Сравнение с прямыми API (если бы использовали):

**Claude 3.5 Sonnet (через Anthropic API):**
- Вход: 40K × $3.00 / 1M = $0.12
- Выход: 3K × $15.00 / 1M = $0.045
- **Итого: ~$0.165 (~17 центов)** 💰💰💰💰

**Claude 3 Opus (через Anthropic API):**
- Вход: 40K × $15.00 / 1M = $0.60
- Выход: 3K × $75.00 / 1M = $0.225
- **Итого: ~$0.825 (~82 цента)** 💰💰💰💰💰💰💰💰

### Экономия от использования Sonar Pro вместо Claude:

**Sonar Pro:** $0.043
**Claude Sonnet:** $0.165

**Экономия: $0.122 (в 3.8 раза дешевле!)**

### Экономия от фильтрации:

Без фильтрации (500 сообщений = ~250K символов = ~65K токенов):
- Sonar Pro: 65K × $1 / 1M + 3K × $1 / 1M = **$0.068**

С фильтрацией (300 сообщений = ~150K символов = ~40K токенов):
- Sonar Pro: 40K × $1 / 1M + 3K × $1 / 1M = **$0.043**

**Экономия: $0.025 (37%) на каждом анализе!**

### Месячная стоимость (2 анализа в день):

**Sonar:**
- ~$0.009 × 2 × 30 = **$0.54 в месяц**

**Sonar Pro:**
- ~$0.043 × 2 × 30 = **$2.58 в месяц**

**Claude Sonnet (если бы был доступен):**
- ~$0.165 × 2 × 30 = **$9.90 в месяц**

💡 **Sonar Pro оптимален по соотношению цена/качество!**

## 🎮 Управление моделями через Telegram

### Посмотреть текущую модель:
```
/show_model
```

### Сменить модель:
```
/set_model claude-3.5-sonnet
```

### Проверить конфигурацию:
```
/config
```

### После изменения MODEL_CONFIG.txt на сервере:
```
/reload_config
```

## 📝 Редактирование конфигурации

### Через Telegram (быстро):
```
/set_model claude-3.5-sonnet
/show_model  # Проверить
```

### Через файл на сервере:
```bash
nano MODEL_CONFIG.txt
# Изменить MODEL=claude-3.5-sonnet
```

В Telegram:
```
/reload_config
```

### Через Git (рекомендуется):
1. Локально отредактировать `MODEL_CONFIG.txt`
2. `git add MODEL_CONFIG.txt && git commit -m "Switch to Claude" && git push`
3. На сервере: `git pull`
4. В Telegram: `/reload_config`

## ⚙️ Про Reasoning режим

**Что это:**
Экспериментальная фича, где модель "думает вслух" перед ответом.

**Статус:**
- ⚠️ Не все модели Perplexity поддерживают
- 🧪 Экспериментально
- 📊 Может улучшить качество анализа
- 💰 Увеличивает стоимость (больше токенов)

**Как включить:**
В `MODEL_CONFIG.txt`:
```
USE_REASONING=true
```

Затем `/reload_config`

**Рекомендация:** 
Пока оставьте `false`, так как Perplexity API может не поддерживать этот параметр для Claude моделей.

## 🎯 Итоговые рекомендации

### Для вашего случая (криптовалютные чаты):

1. **Используйте Sonar Pro** ✅ (уже настроено по умолчанию!)
   ```
   /set_model sonar-pro
   ```

2. **Оптимизируйте период анализа:**
   - Не `/analyze 3d` (слишком много)
   - Лучше `/analyze 12h` или `/analyze 24h`

3. **Проверяйте статистику фильтрации:**
   ```
   ✅ Оптимизация завершена:
      • Экономия: 50 сообщений (33.3%)
   ```

4. **При превышении лимита:**
   - Уменьшите период
   - Или разбейте на несколько анализов

5. **Мониторьте статистику токенов:**
   ```
   📊 Использовано токенов:
      Промпт: 45000
      Ответ: 3500
      Всего: 48500
   ```

6. **Если нужно ещё выше качество:**
   - Рассмотрите использование Claude напрямую через Anthropic API
   - Но помните: Sonar Pro в 3-4 раза дешевле
   - И имеет интеграцию с поиском в реальном времени

## 📚 Дополнительная информация

- **Perplexity API**: https://docs.perplexity.ai
- **Anthropic API** (для Claude): https://www.anthropic.com/api
- **OpenAI API** (для GPT): https://platform.openai.com
- **Конфигурация бота**: См. `CONFIG_MANAGEMENT.md`

## 🔗 Полезные ссылки

- [Perplexity API Documentation](https://docs.perplexity.ai)
- [Sonar Models Overview](https://www.perplexity.ai/hub/blog/introducing-the-sonar-pro-api)
- [Llama 3.3 70B](https://ai.meta.com/llama/) - базовая модель для Sonar

---

**Готово к использованию!** Sonar Pro уже настроен по умолчанию. 🎉

**Ключевые преимущества:**
- ✅ В 3-4 раза дешевле чем Claude
- ✅ Интеграция с поиском Perplexity
- ✅ Основан на Llama 3.3 70B
- ✅ Быстрые ответы
- ✅ Хорошее качество для криптовалютных чатов

